{
  "scripts": {
    "api": "hai-server --watch todos.json --port 8000",
    "start": "lite-server"
  },
  "devDependencies": {
    "hai-server": "^0.0.4",
    "lite-server": "^2.6.1"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
